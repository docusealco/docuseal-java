/**
 * This file was auto-generated by Fern from our API Definition.
 */
package com.docuseal;

import com.docuseal.core.ClientOptions;
import com.docuseal.core.DocusealClientApiException;
import com.docuseal.core.DocusealClientException;
import com.docuseal.core.DocusealClientHttpResponse;
import com.docuseal.core.MediaTypes;
import com.docuseal.core.ObjectMappers;
import com.docuseal.core.QueryStringMapper;
import com.docuseal.core.RequestOptions;
import com.docuseal.core.RetryInterceptor;
import com.docuseal.requests.ArchiveSubmissionParams;
import com.docuseal.requests.ArchiveTemplateParams;
import com.docuseal.requests.CloneTemplateParams;
import com.docuseal.requests.CreateSubmissionFromDocxParams;
import com.docuseal.requests.CreateSubmissionFromHtmlParams;
import com.docuseal.requests.CreateSubmissionFromPdfParams;
import com.docuseal.requests.CreateSubmissionParams;
import com.docuseal.requests.CreateTemplateFromDocxParams;
import com.docuseal.requests.CreateTemplateFromHtmlParams;
import com.docuseal.requests.CreateTemplateFromPdfParams;
import com.docuseal.requests.GetSubmissionDocumentsParams;
import com.docuseal.requests.GetSubmissionParams;
import com.docuseal.requests.GetSubmissionsParams;
import com.docuseal.requests.GetSubmitterParams;
import com.docuseal.requests.GetSubmittersParams;
import com.docuseal.requests.GetTemplateParams;
import com.docuseal.requests.GetTemplatesParams;
import com.docuseal.requests.MergeTemplateParams;
import com.docuseal.requests.PermanentlyDeleteSubmissionParams;
import com.docuseal.requests.PermanentlyDeleteTemplateParams;
import com.docuseal.requests.UpdateSubmissionParams;
import com.docuseal.requests.UpdateSubmitterParams;
import com.docuseal.requests.UpdateTemplateDocumentsParams;
import com.docuseal.requests.UpdateTemplateParams;
import com.docuseal.types.Submission;
import com.docuseal.types.SubmissionArchiveResult;
import com.docuseal.types.SubmissionCreateOneoffResult;
import com.docuseal.types.SubmissionCreateResult;
import com.docuseal.types.SubmissionDocuments;
import com.docuseal.types.SubmissionList;
import com.docuseal.types.SubmissionPermanentlyDeleteResult;
import com.docuseal.types.SubmissionUpdateResult;
import com.docuseal.types.Submitter;
import com.docuseal.types.SubmitterList;
import com.docuseal.types.SubmitterUpdateResult;
import com.docuseal.types.Template;
import com.docuseal.types.TemplateArchiveResult;
import com.docuseal.types.TemplateList;
import com.docuseal.types.TemplatePermanentlyDeleteResult;
import com.docuseal.types.TemplateUpdateResult;
import com.fasterxml.jackson.core.JsonProcessingException;
import java.io.IOException;
import okhttp3.Headers;
import okhttp3.HttpUrl;
import okhttp3.OkHttpClient;
import okhttp3.Request;
import okhttp3.RequestBody;
import okhttp3.Response;
import okhttp3.ResponseBody;

public class RawDocusealClient {
    protected final ClientOptions clientOptions;

    public RawDocusealClient(ClientOptions clientOptions) {
        this.clientOptions = clientOptions;
    }

    /**
     * The API endpoint provides the ability to retrieve a list of available document templates.
     */
    public DocusealClientHttpResponse<TemplateList> getTemplates() {
        return getTemplates(GetTemplatesParams.builder().build());
    }

    /**
     * The API endpoint provides the ability to retrieve a list of available document templates.
     */
    public DocusealClientHttpResponse<TemplateList> getTemplates(RequestOptions requestOptions) {
        return getTemplates(GetTemplatesParams.builder().build(), requestOptions);
    }

    /**
     * The API endpoint provides the ability to retrieve a list of available document templates.
     */
    public DocusealClientHttpResponse<TemplateList> getTemplates(GetTemplatesParams request) {
        return getTemplates(request, null);
    }

    /**
     * The API endpoint provides the ability to retrieve a list of available document templates.
     */
    public DocusealClientHttpResponse<TemplateList> getTemplates(
            GetTemplatesParams request, RequestOptions requestOptions) {
        HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl())
                .newBuilder()
                .addPathSegments("templates");
        if (request.getQ().isPresent()) {
            QueryStringMapper.addQueryParameter(httpUrl, "q", request.getQ().get(), false);
        }
        if (request.getSlug().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "slug", request.getSlug().get(), false);
        }
        if (request.getExternalId().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "external_id", request.getExternalId().get(), false);
        }
        if (request.getFolder().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "folder", request.getFolder().get(), false);
        }
        if (request.getArchived().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "archived", request.getArchived().get(), false);
        }
        if (request.getShared().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "shared", request.getShared().get(), false);
        }
        if (request.getLimit().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "limit", request.getLimit().get(), false);
        }
        if (request.getAfter().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "after", request.getAfter().get(), false);
        }
        if (request.getBefore().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "before", request.getBefore().get(), false);
        }
        if (requestOptions != null) {
            requestOptions.getQueryParameters().forEach((_key, _value) -> {
                httpUrl.addQueryParameter(_key, _value);
            });
        }
        Request.Builder _requestBuilder = new Request.Builder()
                .url(httpUrl.build())
                .method("GET", null)
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Accept", "application/json");
        Request okhttpRequest = _requestBuilder.build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        if (requestOptions != null && requestOptions.getMaxRetries().isPresent()) {
            okhttpRequest = okhttpRequest
                    .newBuilder()
                    .tag(
                            RetryInterceptor.MaxRetriesOverride.class,
                            new RetryInterceptor.MaxRetriesOverride(
                                    requestOptions.getMaxRetries().get()))
                    .build();
        }
        try (Response response = client.newCall(okhttpRequest).execute()) {
            ResponseBody responseBody = response.body();
            String responseBodyString = responseBody != null ? responseBody.string() : "{}";
            if (response.isSuccessful()) {
                return new DocusealClientHttpResponse<>(
                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, TemplateList.class), response);
            }
            Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
            throw new DocusealClientApiException(
                    "Error with status code " + response.code(), response.code(), errorBody, response);
        } catch (JsonProcessingException e) {
            throw new DocusealClientException("Failed to deserialize response: " + e.getMessage(), e);
        } catch (IOException e) {
            throw new DocusealClientException("Network error executing HTTP request", e);
        }
    }

    /**
     * The API endpoint provides the functionality to retrieve information about a document template.
     */
    public DocusealClientHttpResponse<Template> getTemplate(int id) {
        return getTemplate(id, GetTemplateParams.builder().build());
    }

    /**
     * The API endpoint provides the functionality to retrieve information about a document template.
     */
    public DocusealClientHttpResponse<Template> getTemplate(int id, RequestOptions requestOptions) {
        return getTemplate(id, GetTemplateParams.builder().build(), requestOptions);
    }

    /**
     * The API endpoint provides the functionality to retrieve information about a document template.
     */
    public DocusealClientHttpResponse<Template> getTemplate(int id, GetTemplateParams request) {
        return getTemplate(id, request, null);
    }

    /**
     * The API endpoint provides the functionality to retrieve information about a document template.
     */
    public DocusealClientHttpResponse<Template> getTemplate(
            int id, GetTemplateParams request, RequestOptions requestOptions) {
        HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl())
                .newBuilder()
                .addPathSegments("templates")
                .addPathSegment(Integer.toString(id));
        if (requestOptions != null) {
            requestOptions.getQueryParameters().forEach((_key, _value) -> {
                httpUrl.addQueryParameter(_key, _value);
            });
        }
        Request.Builder _requestBuilder = new Request.Builder()
                .url(httpUrl.build())
                .method("GET", null)
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Accept", "application/json");
        Request okhttpRequest = _requestBuilder.build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        if (requestOptions != null && requestOptions.getMaxRetries().isPresent()) {
            okhttpRequest = okhttpRequest
                    .newBuilder()
                    .tag(
                            RetryInterceptor.MaxRetriesOverride.class,
                            new RetryInterceptor.MaxRetriesOverride(
                                    requestOptions.getMaxRetries().get()))
                    .build();
        }
        try (Response response = client.newCall(okhttpRequest).execute()) {
            ResponseBody responseBody = response.body();
            String responseBodyString = responseBody != null ? responseBody.string() : "{}";
            if (response.isSuccessful()) {
                return new DocusealClientHttpResponse<>(
                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Template.class), response);
            }
            Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
            throw new DocusealClientApiException(
                    "Error with status code " + response.code(), response.code(), errorBody, response);
        } catch (JsonProcessingException e) {
            throw new DocusealClientException("Failed to deserialize response: " + e.getMessage(), e);
        } catch (IOException e) {
            throw new DocusealClientException("Network error executing HTTP request", e);
        }
    }

    /**
     * The API endpoint provides the functionality to move a document template to a different folder and update the name of the template.
     */
    public DocusealClientHttpResponse<TemplateUpdateResult> updateTemplate(int id) {
        return updateTemplate(id, UpdateTemplateParams.builder().build());
    }

    /**
     * The API endpoint provides the functionality to move a document template to a different folder and update the name of the template.
     */
    public DocusealClientHttpResponse<TemplateUpdateResult> updateTemplate(int id, RequestOptions requestOptions) {
        return updateTemplate(id, UpdateTemplateParams.builder().build(), requestOptions);
    }

    /**
     * The API endpoint provides the functionality to move a document template to a different folder and update the name of the template.
     */
    public DocusealClientHttpResponse<TemplateUpdateResult> updateTemplate(int id, UpdateTemplateParams request) {
        return updateTemplate(id, request, null);
    }

    /**
     * The API endpoint provides the functionality to move a document template to a different folder and update the name of the template.
     */
    public DocusealClientHttpResponse<TemplateUpdateResult> updateTemplate(
            int id, UpdateTemplateParams request, RequestOptions requestOptions) {
        HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl())
                .newBuilder()
                .addPathSegments("templates")
                .addPathSegment(Integer.toString(id));
        if (requestOptions != null) {
            requestOptions.getQueryParameters().forEach((_key, _value) -> {
                httpUrl.addQueryParameter(_key, _value);
            });
        }
        RequestBody body;
        try {
            body = RequestBody.create(
                    ObjectMappers.JSON_MAPPER.writeValueAsBytes(request), MediaTypes.APPLICATION_JSON);
        } catch (JsonProcessingException e) {
            throw new DocusealClientException("Failed to serialize request", e);
        }
        Request okhttpRequest = new Request.Builder()
                .url(httpUrl.build())
                .method("PUT", body)
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Content-Type", "application/json")
                .addHeader("Accept", "application/json")
                .build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        if (requestOptions != null && requestOptions.getMaxRetries().isPresent()) {
            okhttpRequest = okhttpRequest
                    .newBuilder()
                    .tag(
                            RetryInterceptor.MaxRetriesOverride.class,
                            new RetryInterceptor.MaxRetriesOverride(
                                    requestOptions.getMaxRetries().get()))
                    .build();
        }
        try (Response response = client.newCall(okhttpRequest).execute()) {
            ResponseBody responseBody = response.body();
            String responseBodyString = responseBody != null ? responseBody.string() : "{}";
            if (response.isSuccessful()) {
                return new DocusealClientHttpResponse<>(
                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, TemplateUpdateResult.class), response);
            }
            Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
            throw new DocusealClientApiException(
                    "Error with status code " + response.code(), response.code(), errorBody, response);
        } catch (JsonProcessingException e) {
            throw new DocusealClientException("Failed to deserialize response: " + e.getMessage(), e);
        } catch (IOException e) {
            throw new DocusealClientException("Network error executing HTTP request", e);
        }
    }

    /**
     * The API endpoint allows you to archive a document template.
     */
    public DocusealClientHttpResponse<TemplateArchiveResult> archiveTemplate(int id) {
        return archiveTemplate(id, ArchiveTemplateParams.builder().build());
    }

    /**
     * The API endpoint allows you to archive a document template.
     */
    public DocusealClientHttpResponse<TemplateArchiveResult> archiveTemplate(int id, RequestOptions requestOptions) {
        return archiveTemplate(id, ArchiveTemplateParams.builder().build(), requestOptions);
    }

    /**
     * The API endpoint allows you to archive a document template.
     */
    public DocusealClientHttpResponse<TemplateArchiveResult> archiveTemplate(int id, ArchiveTemplateParams request) {
        return archiveTemplate(id, request, null);
    }

    /**
     * The API endpoint allows you to archive a document template.
     */
    public DocusealClientHttpResponse<TemplateArchiveResult> archiveTemplate(
            int id, ArchiveTemplateParams request, RequestOptions requestOptions) {
        HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl())
                .newBuilder()
                .addPathSegments("templates")
                .addPathSegment(Integer.toString(id));
        if (requestOptions != null) {
            requestOptions.getQueryParameters().forEach((_key, _value) -> {
                httpUrl.addQueryParameter(_key, _value);
            });
        }
        Request.Builder _requestBuilder = new Request.Builder()
                .url(httpUrl.build())
                .method("DELETE", null)
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Accept", "application/json");
        Request okhttpRequest = _requestBuilder.build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        if (requestOptions != null && requestOptions.getMaxRetries().isPresent()) {
            okhttpRequest = okhttpRequest
                    .newBuilder()
                    .tag(
                            RetryInterceptor.MaxRetriesOverride.class,
                            new RetryInterceptor.MaxRetriesOverride(
                                    requestOptions.getMaxRetries().get()))
                    .build();
        }
        try (Response response = client.newCall(okhttpRequest).execute()) {
            ResponseBody responseBody = response.body();
            String responseBodyString = responseBody != null ? responseBody.string() : "{}";
            if (response.isSuccessful()) {
                return new DocusealClientHttpResponse<>(
                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, TemplateArchiveResult.class), response);
            }
            Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
            throw new DocusealClientApiException(
                    "Error with status code " + response.code(), response.code(), errorBody, response);
        } catch (JsonProcessingException e) {
            throw new DocusealClientException("Failed to deserialize response: " + e.getMessage(), e);
        } catch (IOException e) {
            throw new DocusealClientException("Network error executing HTTP request", e);
        }
    }

    /**
     * The API endpoint provides the ability to retrieve a list of available submissions.
     */
    public DocusealClientHttpResponse<SubmissionList> getSubmissions() {
        return getSubmissions(GetSubmissionsParams.builder().build());
    }

    /**
     * The API endpoint provides the ability to retrieve a list of available submissions.
     */
    public DocusealClientHttpResponse<SubmissionList> getSubmissions(RequestOptions requestOptions) {
        return getSubmissions(GetSubmissionsParams.builder().build(), requestOptions);
    }

    /**
     * The API endpoint provides the ability to retrieve a list of available submissions.
     */
    public DocusealClientHttpResponse<SubmissionList> getSubmissions(GetSubmissionsParams request) {
        return getSubmissions(request, null);
    }

    /**
     * The API endpoint provides the ability to retrieve a list of available submissions.
     */
    public DocusealClientHttpResponse<SubmissionList> getSubmissions(
            GetSubmissionsParams request, RequestOptions requestOptions) {
        HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl())
                .newBuilder()
                .addPathSegments("submissions");
        if (request.getTemplateId().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "template_id", request.getTemplateId().get(), false);
        }
        if (request.getStatus().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "status", request.getStatus().get(), false);
        }
        if (request.getQ().isPresent()) {
            QueryStringMapper.addQueryParameter(httpUrl, "q", request.getQ().get(), false);
        }
        if (request.getSlug().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "slug", request.getSlug().get(), false);
        }
        if (request.getTemplateFolder().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "template_folder", request.getTemplateFolder().get(), false);
        }
        if (request.getArchived().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "archived", request.getArchived().get(), false);
        }
        if (request.getLimit().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "limit", request.getLimit().get(), false);
        }
        if (request.getAfter().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "after", request.getAfter().get(), false);
        }
        if (request.getBefore().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "before", request.getBefore().get(), false);
        }
        if (requestOptions != null) {
            requestOptions.getQueryParameters().forEach((_key, _value) -> {
                httpUrl.addQueryParameter(_key, _value);
            });
        }
        Request.Builder _requestBuilder = new Request.Builder()
                .url(httpUrl.build())
                .method("GET", null)
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Accept", "application/json");
        Request okhttpRequest = _requestBuilder.build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        if (requestOptions != null && requestOptions.getMaxRetries().isPresent()) {
            okhttpRequest = okhttpRequest
                    .newBuilder()
                    .tag(
                            RetryInterceptor.MaxRetriesOverride.class,
                            new RetryInterceptor.MaxRetriesOverride(
                                    requestOptions.getMaxRetries().get()))
                    .build();
        }
        try (Response response = client.newCall(okhttpRequest).execute()) {
            ResponseBody responseBody = response.body();
            String responseBodyString = responseBody != null ? responseBody.string() : "{}";
            if (response.isSuccessful()) {
                return new DocusealClientHttpResponse<>(
                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, SubmissionList.class), response);
            }
            Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
            throw new DocusealClientApiException(
                    "Error with status code " + response.code(), response.code(), errorBody, response);
        } catch (JsonProcessingException e) {
            throw new DocusealClientException("Failed to deserialize response: " + e.getMessage(), e);
        } catch (IOException e) {
            throw new DocusealClientException("Network error executing HTTP request", e);
        }
    }

    /**
     * The API endpoint provides the functionality to retrieve information about a submission.
     */
    public DocusealClientHttpResponse<Submission> getSubmission(int id) {
        return getSubmission(id, GetSubmissionParams.builder().build());
    }

    /**
     * The API endpoint provides the functionality to retrieve information about a submission.
     */
    public DocusealClientHttpResponse<Submission> getSubmission(int id, RequestOptions requestOptions) {
        return getSubmission(id, GetSubmissionParams.builder().build(), requestOptions);
    }

    /**
     * The API endpoint provides the functionality to retrieve information about a submission.
     */
    public DocusealClientHttpResponse<Submission> getSubmission(int id, GetSubmissionParams request) {
        return getSubmission(id, request, null);
    }

    /**
     * The API endpoint provides the functionality to retrieve information about a submission.
     */
    public DocusealClientHttpResponse<Submission> getSubmission(
            int id, GetSubmissionParams request, RequestOptions requestOptions) {
        HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl())
                .newBuilder()
                .addPathSegments("submissions")
                .addPathSegment(Integer.toString(id));
        if (requestOptions != null) {
            requestOptions.getQueryParameters().forEach((_key, _value) -> {
                httpUrl.addQueryParameter(_key, _value);
            });
        }
        Request.Builder _requestBuilder = new Request.Builder()
                .url(httpUrl.build())
                .method("GET", null)
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Accept", "application/json");
        Request okhttpRequest = _requestBuilder.build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        if (requestOptions != null && requestOptions.getMaxRetries().isPresent()) {
            okhttpRequest = okhttpRequest
                    .newBuilder()
                    .tag(
                            RetryInterceptor.MaxRetriesOverride.class,
                            new RetryInterceptor.MaxRetriesOverride(
                                    requestOptions.getMaxRetries().get()))
                    .build();
        }
        try (Response response = client.newCall(okhttpRequest).execute()) {
            ResponseBody responseBody = response.body();
            String responseBodyString = responseBody != null ? responseBody.string() : "{}";
            if (response.isSuccessful()) {
                return new DocusealClientHttpResponse<>(
                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Submission.class), response);
            }
            Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
            throw new DocusealClientApiException(
                    "Error with status code " + response.code(), response.code(), errorBody, response);
        } catch (JsonProcessingException e) {
            throw new DocusealClientException("Failed to deserialize response: " + e.getMessage(), e);
        } catch (IOException e) {
            throw new DocusealClientException("Network error executing HTTP request", e);
        }
    }

    /**
     * The API endpoint allows you to update a submission: change its name, expiration date, and archive or unarchive it.
     */
    public DocusealClientHttpResponse<SubmissionUpdateResult> updateSubmission(int id) {
        return updateSubmission(id, UpdateSubmissionParams.builder().build());
    }

    /**
     * The API endpoint allows you to update a submission: change its name, expiration date, and archive or unarchive it.
     */
    public DocusealClientHttpResponse<SubmissionUpdateResult> updateSubmission(int id, RequestOptions requestOptions) {
        return updateSubmission(id, UpdateSubmissionParams.builder().build(), requestOptions);
    }

    /**
     * The API endpoint allows you to update a submission: change its name, expiration date, and archive or unarchive it.
     */
    public DocusealClientHttpResponse<SubmissionUpdateResult> updateSubmission(int id, UpdateSubmissionParams request) {
        return updateSubmission(id, request, null);
    }

    /**
     * The API endpoint allows you to update a submission: change its name, expiration date, and archive or unarchive it.
     */
    public DocusealClientHttpResponse<SubmissionUpdateResult> updateSubmission(
            int id, UpdateSubmissionParams request, RequestOptions requestOptions) {
        HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl())
                .newBuilder()
                .addPathSegments("submissions")
                .addPathSegment(Integer.toString(id));
        if (requestOptions != null) {
            requestOptions.getQueryParameters().forEach((_key, _value) -> {
                httpUrl.addQueryParameter(_key, _value);
            });
        }
        RequestBody body;
        try {
            body = RequestBody.create(
                    ObjectMappers.JSON_MAPPER.writeValueAsBytes(request), MediaTypes.APPLICATION_JSON);
        } catch (JsonProcessingException e) {
            throw new DocusealClientException("Failed to serialize request", e);
        }
        Request okhttpRequest = new Request.Builder()
                .url(httpUrl.build())
                .method("PUT", body)
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Content-Type", "application/json")
                .addHeader("Accept", "application/json")
                .build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        if (requestOptions != null && requestOptions.getMaxRetries().isPresent()) {
            okhttpRequest = okhttpRequest
                    .newBuilder()
                    .tag(
                            RetryInterceptor.MaxRetriesOverride.class,
                            new RetryInterceptor.MaxRetriesOverride(
                                    requestOptions.getMaxRetries().get()))
                    .build();
        }
        try (Response response = client.newCall(okhttpRequest).execute()) {
            ResponseBody responseBody = response.body();
            String responseBodyString = responseBody != null ? responseBody.string() : "{}";
            if (response.isSuccessful()) {
                return new DocusealClientHttpResponse<>(
                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, SubmissionUpdateResult.class),
                        response);
            }
            Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
            throw new DocusealClientApiException(
                    "Error with status code " + response.code(), response.code(), errorBody, response);
        } catch (JsonProcessingException e) {
            throw new DocusealClientException("Failed to deserialize response: " + e.getMessage(), e);
        } catch (IOException e) {
            throw new DocusealClientException("Network error executing HTTP request", e);
        }
    }

    /**
     * The API endpoint allows you to archive a submission.
     */
    public DocusealClientHttpResponse<SubmissionArchiveResult> archiveSubmission(int id) {
        return archiveSubmission(id, ArchiveSubmissionParams.builder().build());
    }

    /**
     * The API endpoint allows you to archive a submission.
     */
    public DocusealClientHttpResponse<SubmissionArchiveResult> archiveSubmission(
            int id, RequestOptions requestOptions) {
        return archiveSubmission(id, ArchiveSubmissionParams.builder().build(), requestOptions);
    }

    /**
     * The API endpoint allows you to archive a submission.
     */
    public DocusealClientHttpResponse<SubmissionArchiveResult> archiveSubmission(
            int id, ArchiveSubmissionParams request) {
        return archiveSubmission(id, request, null);
    }

    /**
     * The API endpoint allows you to archive a submission.
     */
    public DocusealClientHttpResponse<SubmissionArchiveResult> archiveSubmission(
            int id, ArchiveSubmissionParams request, RequestOptions requestOptions) {
        HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl())
                .newBuilder()
                .addPathSegments("submissions")
                .addPathSegment(Integer.toString(id));
        if (requestOptions != null) {
            requestOptions.getQueryParameters().forEach((_key, _value) -> {
                httpUrl.addQueryParameter(_key, _value);
            });
        }
        Request.Builder _requestBuilder = new Request.Builder()
                .url(httpUrl.build())
                .method("DELETE", null)
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Accept", "application/json");
        Request okhttpRequest = _requestBuilder.build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        if (requestOptions != null && requestOptions.getMaxRetries().isPresent()) {
            okhttpRequest = okhttpRequest
                    .newBuilder()
                    .tag(
                            RetryInterceptor.MaxRetriesOverride.class,
                            new RetryInterceptor.MaxRetriesOverride(
                                    requestOptions.getMaxRetries().get()))
                    .build();
        }
        try (Response response = client.newCall(okhttpRequest).execute()) {
            ResponseBody responseBody = response.body();
            String responseBodyString = responseBody != null ? responseBody.string() : "{}";
            if (response.isSuccessful()) {
                return new DocusealClientHttpResponse<>(
                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, SubmissionArchiveResult.class),
                        response);
            }
            Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
            throw new DocusealClientApiException(
                    "Error with status code " + response.code(), response.code(), errorBody, response);
        } catch (JsonProcessingException e) {
            throw new DocusealClientException("Failed to deserialize response: " + e.getMessage(), e);
        } catch (IOException e) {
            throw new DocusealClientException("Network error executing HTTP request", e);
        }
    }

    /**
     * This endpoint returns a list of partially filled documents for a submission. If the submission has been completed, the final signed documents are returned.
     */
    public DocusealClientHttpResponse<SubmissionDocuments> getSubmissionDocuments(int id) {
        return getSubmissionDocuments(id, GetSubmissionDocumentsParams.builder().build());
    }

    /**
     * This endpoint returns a list of partially filled documents for a submission. If the submission has been completed, the final signed documents are returned.
     */
    public DocusealClientHttpResponse<SubmissionDocuments> getSubmissionDocuments(
            int id, RequestOptions requestOptions) {
        return getSubmissionDocuments(id, GetSubmissionDocumentsParams.builder().build(), requestOptions);
    }

    /**
     * This endpoint returns a list of partially filled documents for a submission. If the submission has been completed, the final signed documents are returned.
     */
    public DocusealClientHttpResponse<SubmissionDocuments> getSubmissionDocuments(
            int id, GetSubmissionDocumentsParams request) {
        return getSubmissionDocuments(id, request, null);
    }

    /**
     * This endpoint returns a list of partially filled documents for a submission. If the submission has been completed, the final signed documents are returned.
     */
    public DocusealClientHttpResponse<SubmissionDocuments> getSubmissionDocuments(
            int id, GetSubmissionDocumentsParams request, RequestOptions requestOptions) {
        HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl())
                .newBuilder()
                .addPathSegments("submissions")
                .addPathSegment(Integer.toString(id))
                .addPathSegments("documents");
        if (request.getMerge().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "merge", request.getMerge().get(), false);
        }
        if (requestOptions != null) {
            requestOptions.getQueryParameters().forEach((_key, _value) -> {
                httpUrl.addQueryParameter(_key, _value);
            });
        }
        Request.Builder _requestBuilder = new Request.Builder()
                .url(httpUrl.build())
                .method("GET", null)
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Accept", "application/json");
        Request okhttpRequest = _requestBuilder.build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        if (requestOptions != null && requestOptions.getMaxRetries().isPresent()) {
            okhttpRequest = okhttpRequest
                    .newBuilder()
                    .tag(
                            RetryInterceptor.MaxRetriesOverride.class,
                            new RetryInterceptor.MaxRetriesOverride(
                                    requestOptions.getMaxRetries().get()))
                    .build();
        }
        try (Response response = client.newCall(okhttpRequest).execute()) {
            ResponseBody responseBody = response.body();
            String responseBodyString = responseBody != null ? responseBody.string() : "{}";
            if (response.isSuccessful()) {
                return new DocusealClientHttpResponse<>(
                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, SubmissionDocuments.class), response);
            }
            Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
            throw new DocusealClientApiException(
                    "Error with status code " + response.code(), response.code(), errorBody, response);
        } catch (JsonProcessingException e) {
            throw new DocusealClientException("Failed to deserialize response: " + e.getMessage(), e);
        } catch (IOException e) {
            throw new DocusealClientException("Network error executing HTTP request", e);
        }
    }

    /**
     * The API endpoint provides the functionality to create one-off submission request from a PDF. Use <code>{{Field Name;role=Signer1;type=date}}</code> text tags to define fillable fields in the document. See <a href="https://www.docuseal.com/examples/fieldtags.pdf" target="_blank" class="link font-bold">https://www.docuseal.com/examples/fieldtags.pdf</a> for more text tag formats. Or specify the exact pixel coordinates of the document fields using <code>fields</code> param.<br><b>Related Guides</b><br><a href="https://www.docuseal.com/guides/use-embedded-text-field-tags-in-the-pdf-to-create-a-fillable-form" class="link">Use embedded text field tags to create a fillable form</a>
     */
    public DocusealClientHttpResponse<SubmissionCreateOneoffResult> createSubmissionFromPdf(
            CreateSubmissionFromPdfParams request) {
        return createSubmissionFromPdf(request, null);
    }

    /**
     * The API endpoint provides the functionality to create one-off submission request from a PDF. Use <code>{{Field Name;role=Signer1;type=date}}</code> text tags to define fillable fields in the document. See <a href="https://www.docuseal.com/examples/fieldtags.pdf" target="_blank" class="link font-bold">https://www.docuseal.com/examples/fieldtags.pdf</a> for more text tag formats. Or specify the exact pixel coordinates of the document fields using <code>fields</code> param.<br><b>Related Guides</b><br><a href="https://www.docuseal.com/guides/use-embedded-text-field-tags-in-the-pdf-to-create-a-fillable-form" class="link">Use embedded text field tags to create a fillable form</a>
     */
    public DocusealClientHttpResponse<SubmissionCreateOneoffResult> createSubmissionFromPdf(
            CreateSubmissionFromPdfParams request, RequestOptions requestOptions) {
        HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl())
                .newBuilder()
                .addPathSegments("submissions/pdf");
        if (requestOptions != null) {
            requestOptions.getQueryParameters().forEach((_key, _value) -> {
                httpUrl.addQueryParameter(_key, _value);
            });
        }
        RequestBody body;
        try {
            body = RequestBody.create(
                    ObjectMappers.JSON_MAPPER.writeValueAsBytes(request), MediaTypes.APPLICATION_JSON);
        } catch (JsonProcessingException e) {
            throw new DocusealClientException("Failed to serialize request", e);
        }
        Request okhttpRequest = new Request.Builder()
                .url(httpUrl.build())
                .method("POST", body)
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Content-Type", "application/json")
                .addHeader("Accept", "application/json")
                .build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        if (requestOptions != null && requestOptions.getMaxRetries().isPresent()) {
            okhttpRequest = okhttpRequest
                    .newBuilder()
                    .tag(
                            RetryInterceptor.MaxRetriesOverride.class,
                            new RetryInterceptor.MaxRetriesOverride(
                                    requestOptions.getMaxRetries().get()))
                    .build();
        }
        try (Response response = client.newCall(okhttpRequest).execute()) {
            ResponseBody responseBody = response.body();
            String responseBodyString = responseBody != null ? responseBody.string() : "{}";
            if (response.isSuccessful()) {
                return new DocusealClientHttpResponse<>(
                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, SubmissionCreateOneoffResult.class),
                        response);
            }
            Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
            throw new DocusealClientApiException(
                    "Error with status code " + response.code(), response.code(), errorBody, response);
        } catch (JsonProcessingException e) {
            throw new DocusealClientException("Failed to deserialize response: " + e.getMessage(), e);
        } catch (IOException e) {
            throw new DocusealClientException("Network error executing HTTP request", e);
        }
    }

    /**
     * The API endpoint provides functionality to create a one-off submission request from a DOCX file with dynamic content variables. Use <code>[[variable_name]]</code> text tags to define dynamic content variables in the document. See <a href="https://www.docuseal.com/examples/demo_template.docx" target="_blank" class="link font-bold">https://www.docuseal.com/examples/demo_template.docx</a> for the specific text variable syntax, including dynamic content tables and lists. You can also use the <code>{{signature}}</code> field syntax to define fillable fields, as in a PDF.<br><b>Related Guides</b><br><a href="https://www.docuseal.com/guides/use-dynamic-content-variables-in-docx-to-create-personalized-documents" class="link">Use dynamic content variables in DOCX to create personalized documents</a>
     */
    public DocusealClientHttpResponse<SubmissionCreateOneoffResult> createSubmissionFromDocx(
            CreateSubmissionFromDocxParams request) {
        return createSubmissionFromDocx(request, null);
    }

    /**
     * The API endpoint provides functionality to create a one-off submission request from a DOCX file with dynamic content variables. Use <code>[[variable_name]]</code> text tags to define dynamic content variables in the document. See <a href="https://www.docuseal.com/examples/demo_template.docx" target="_blank" class="link font-bold">https://www.docuseal.com/examples/demo_template.docx</a> for the specific text variable syntax, including dynamic content tables and lists. You can also use the <code>{{signature}}</code> field syntax to define fillable fields, as in a PDF.<br><b>Related Guides</b><br><a href="https://www.docuseal.com/guides/use-dynamic-content-variables-in-docx-to-create-personalized-documents" class="link">Use dynamic content variables in DOCX to create personalized documents</a>
     */
    public DocusealClientHttpResponse<SubmissionCreateOneoffResult> createSubmissionFromDocx(
            CreateSubmissionFromDocxParams request, RequestOptions requestOptions) {
        HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl())
                .newBuilder()
                .addPathSegments("submissions/docx");
        if (requestOptions != null) {
            requestOptions.getQueryParameters().forEach((_key, _value) -> {
                httpUrl.addQueryParameter(_key, _value);
            });
        }
        RequestBody body;
        try {
            body = RequestBody.create(
                    ObjectMappers.JSON_MAPPER.writeValueAsBytes(request), MediaTypes.APPLICATION_JSON);
        } catch (JsonProcessingException e) {
            throw new DocusealClientException("Failed to serialize request", e);
        }
        Request okhttpRequest = new Request.Builder()
                .url(httpUrl.build())
                .method("POST", body)
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Content-Type", "application/json")
                .addHeader("Accept", "application/json")
                .build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        if (requestOptions != null && requestOptions.getMaxRetries().isPresent()) {
            okhttpRequest = okhttpRequest
                    .newBuilder()
                    .tag(
                            RetryInterceptor.MaxRetriesOverride.class,
                            new RetryInterceptor.MaxRetriesOverride(
                                    requestOptions.getMaxRetries().get()))
                    .build();
        }
        try (Response response = client.newCall(okhttpRequest).execute()) {
            ResponseBody responseBody = response.body();
            String responseBodyString = responseBody != null ? responseBody.string() : "{}";
            if (response.isSuccessful()) {
                return new DocusealClientHttpResponse<>(
                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, SubmissionCreateOneoffResult.class),
                        response);
            }
            Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
            throw new DocusealClientApiException(
                    "Error with status code " + response.code(), response.code(), errorBody, response);
        } catch (JsonProcessingException e) {
            throw new DocusealClientException("Failed to deserialize response: " + e.getMessage(), e);
        } catch (IOException e) {
            throw new DocusealClientException("Network error executing HTTP request", e);
        }
    }

    /**
     * This API endpoint allows you to create a one-off submission request document using the provided HTML content, with special field tags rendered as a fillable and signable form.<br><b>Related Guides</b><br><a href="https://www.docuseal.com/guides/create-pdf-document-fillable-form-with-html-api" class="link">Create PDF document fillable form with HTML</a>
     */
    public DocusealClientHttpResponse<SubmissionCreateOneoffResult> createSubmissionFromHtml(
            CreateSubmissionFromHtmlParams request) {
        return createSubmissionFromHtml(request, null);
    }

    /**
     * This API endpoint allows you to create a one-off submission request document using the provided HTML content, with special field tags rendered as a fillable and signable form.<br><b>Related Guides</b><br><a href="https://www.docuseal.com/guides/create-pdf-document-fillable-form-with-html-api" class="link">Create PDF document fillable form with HTML</a>
     */
    public DocusealClientHttpResponse<SubmissionCreateOneoffResult> createSubmissionFromHtml(
            CreateSubmissionFromHtmlParams request, RequestOptions requestOptions) {
        HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl())
                .newBuilder()
                .addPathSegments("submissions/html");
        if (requestOptions != null) {
            requestOptions.getQueryParameters().forEach((_key, _value) -> {
                httpUrl.addQueryParameter(_key, _value);
            });
        }
        RequestBody body;
        try {
            body = RequestBody.create(
                    ObjectMappers.JSON_MAPPER.writeValueAsBytes(request), MediaTypes.APPLICATION_JSON);
        } catch (JsonProcessingException e) {
            throw new DocusealClientException("Failed to serialize request", e);
        }
        Request okhttpRequest = new Request.Builder()
                .url(httpUrl.build())
                .method("POST", body)
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Content-Type", "application/json")
                .addHeader("Accept", "application/json")
                .build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        if (requestOptions != null && requestOptions.getMaxRetries().isPresent()) {
            okhttpRequest = okhttpRequest
                    .newBuilder()
                    .tag(
                            RetryInterceptor.MaxRetriesOverride.class,
                            new RetryInterceptor.MaxRetriesOverride(
                                    requestOptions.getMaxRetries().get()))
                    .build();
        }
        try (Response response = client.newCall(okhttpRequest).execute()) {
            ResponseBody responseBody = response.body();
            String responseBodyString = responseBody != null ? responseBody.string() : "{}";
            if (response.isSuccessful()) {
                return new DocusealClientHttpResponse<>(
                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, SubmissionCreateOneoffResult.class),
                        response);
            }
            Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
            throw new DocusealClientApiException(
                    "Error with status code " + response.code(), response.code(), errorBody, response);
        } catch (JsonProcessingException e) {
            throw new DocusealClientException("Failed to deserialize response: " + e.getMessage(), e);
        } catch (IOException e) {
            throw new DocusealClientException("Network error executing HTTP request", e);
        }
    }

    /**
     * The API endpoint provides functionality to retrieve information about a submitter, along with the submitter documents and field values.
     */
    public DocusealClientHttpResponse<Submitter> getSubmitter(int id) {
        return getSubmitter(id, GetSubmitterParams.builder().build());
    }

    /**
     * The API endpoint provides functionality to retrieve information about a submitter, along with the submitter documents and field values.
     */
    public DocusealClientHttpResponse<Submitter> getSubmitter(int id, RequestOptions requestOptions) {
        return getSubmitter(id, GetSubmitterParams.builder().build(), requestOptions);
    }

    /**
     * The API endpoint provides functionality to retrieve information about a submitter, along with the submitter documents and field values.
     */
    public DocusealClientHttpResponse<Submitter> getSubmitter(int id, GetSubmitterParams request) {
        return getSubmitter(id, request, null);
    }

    /**
     * The API endpoint provides functionality to retrieve information about a submitter, along with the submitter documents and field values.
     */
    public DocusealClientHttpResponse<Submitter> getSubmitter(
            int id, GetSubmitterParams request, RequestOptions requestOptions) {
        HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl())
                .newBuilder()
                .addPathSegments("submitters")
                .addPathSegment(Integer.toString(id));
        if (requestOptions != null) {
            requestOptions.getQueryParameters().forEach((_key, _value) -> {
                httpUrl.addQueryParameter(_key, _value);
            });
        }
        Request.Builder _requestBuilder = new Request.Builder()
                .url(httpUrl.build())
                .method("GET", null)
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Accept", "application/json");
        Request okhttpRequest = _requestBuilder.build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        if (requestOptions != null && requestOptions.getMaxRetries().isPresent()) {
            okhttpRequest = okhttpRequest
                    .newBuilder()
                    .tag(
                            RetryInterceptor.MaxRetriesOverride.class,
                            new RetryInterceptor.MaxRetriesOverride(
                                    requestOptions.getMaxRetries().get()))
                    .build();
        }
        try (Response response = client.newCall(okhttpRequest).execute()) {
            ResponseBody responseBody = response.body();
            String responseBodyString = responseBody != null ? responseBody.string() : "{}";
            if (response.isSuccessful()) {
                return new DocusealClientHttpResponse<>(
                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Submitter.class), response);
            }
            Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
            throw new DocusealClientApiException(
                    "Error with status code " + response.code(), response.code(), errorBody, response);
        } catch (JsonProcessingException e) {
            throw new DocusealClientException("Failed to deserialize response: " + e.getMessage(), e);
        } catch (IOException e) {
            throw new DocusealClientException("Network error executing HTTP request", e);
        }
    }

    /**
     * The API endpoint allows you to update submitter details, pre-fill or update field values and re-send emails.<br><b>Related Guides</b><br><a href="https://www.docuseal.com/guides/pre-fill-pdf-document-form-fields-with-api#automatically_sign_documents_via_api" class="link">Automatically sign documents via API</a>
     */
    public DocusealClientHttpResponse<SubmitterUpdateResult> updateSubmitter(int id) {
        return updateSubmitter(id, UpdateSubmitterParams.builder().build());
    }

    /**
     * The API endpoint allows you to update submitter details, pre-fill or update field values and re-send emails.<br><b>Related Guides</b><br><a href="https://www.docuseal.com/guides/pre-fill-pdf-document-form-fields-with-api#automatically_sign_documents_via_api" class="link">Automatically sign documents via API</a>
     */
    public DocusealClientHttpResponse<SubmitterUpdateResult> updateSubmitter(int id, RequestOptions requestOptions) {
        return updateSubmitter(id, UpdateSubmitterParams.builder().build(), requestOptions);
    }

    /**
     * The API endpoint allows you to update submitter details, pre-fill or update field values and re-send emails.<br><b>Related Guides</b><br><a href="https://www.docuseal.com/guides/pre-fill-pdf-document-form-fields-with-api#automatically_sign_documents_via_api" class="link">Automatically sign documents via API</a>
     */
    public DocusealClientHttpResponse<SubmitterUpdateResult> updateSubmitter(int id, UpdateSubmitterParams request) {
        return updateSubmitter(id, request, null);
    }

    /**
     * The API endpoint allows you to update submitter details, pre-fill or update field values and re-send emails.<br><b>Related Guides</b><br><a href="https://www.docuseal.com/guides/pre-fill-pdf-document-form-fields-with-api#automatically_sign_documents_via_api" class="link">Automatically sign documents via API</a>
     */
    public DocusealClientHttpResponse<SubmitterUpdateResult> updateSubmitter(
            int id, UpdateSubmitterParams request, RequestOptions requestOptions) {
        HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl())
                .newBuilder()
                .addPathSegments("submitters")
                .addPathSegment(Integer.toString(id));
        if (requestOptions != null) {
            requestOptions.getQueryParameters().forEach((_key, _value) -> {
                httpUrl.addQueryParameter(_key, _value);
            });
        }
        RequestBody body;
        try {
            body = RequestBody.create(
                    ObjectMappers.JSON_MAPPER.writeValueAsBytes(request), MediaTypes.APPLICATION_JSON);
        } catch (JsonProcessingException e) {
            throw new DocusealClientException("Failed to serialize request", e);
        }
        Request okhttpRequest = new Request.Builder()
                .url(httpUrl.build())
                .method("PUT", body)
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Content-Type", "application/json")
                .addHeader("Accept", "application/json")
                .build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        if (requestOptions != null && requestOptions.getMaxRetries().isPresent()) {
            okhttpRequest = okhttpRequest
                    .newBuilder()
                    .tag(
                            RetryInterceptor.MaxRetriesOverride.class,
                            new RetryInterceptor.MaxRetriesOverride(
                                    requestOptions.getMaxRetries().get()))
                    .build();
        }
        try (Response response = client.newCall(okhttpRequest).execute()) {
            ResponseBody responseBody = response.body();
            String responseBodyString = responseBody != null ? responseBody.string() : "{}";
            if (response.isSuccessful()) {
                return new DocusealClientHttpResponse<>(
                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, SubmitterUpdateResult.class), response);
            }
            Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
            throw new DocusealClientApiException(
                    "Error with status code " + response.code(), response.code(), errorBody, response);
        } catch (JsonProcessingException e) {
            throw new DocusealClientException("Failed to deserialize response: " + e.getMessage(), e);
        } catch (IOException e) {
            throw new DocusealClientException("Network error executing HTTP request", e);
        }
    }

    /**
     * The API endpoint provides the ability to retrieve a list of submitters.
     */
    public DocusealClientHttpResponse<SubmitterList> getSubmitters() {
        return getSubmitters(GetSubmittersParams.builder().build());
    }

    /**
     * The API endpoint provides the ability to retrieve a list of submitters.
     */
    public DocusealClientHttpResponse<SubmitterList> getSubmitters(RequestOptions requestOptions) {
        return getSubmitters(GetSubmittersParams.builder().build(), requestOptions);
    }

    /**
     * The API endpoint provides the ability to retrieve a list of submitters.
     */
    public DocusealClientHttpResponse<SubmitterList> getSubmitters(GetSubmittersParams request) {
        return getSubmitters(request, null);
    }

    /**
     * The API endpoint provides the ability to retrieve a list of submitters.
     */
    public DocusealClientHttpResponse<SubmitterList> getSubmitters(
            GetSubmittersParams request, RequestOptions requestOptions) {
        HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl())
                .newBuilder()
                .addPathSegments("submitters");
        if (request.getSubmissionId().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "submission_id", request.getSubmissionId().get(), false);
        }
        if (request.getQ().isPresent()) {
            QueryStringMapper.addQueryParameter(httpUrl, "q", request.getQ().get(), false);
        }
        if (request.getSlug().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "slug", request.getSlug().get(), false);
        }
        if (request.getCompletedAfter().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "completed_after", request.getCompletedAfter().get(), false);
        }
        if (request.getCompletedBefore().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "completed_before", request.getCompletedBefore().get(), false);
        }
        if (request.getExternalId().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "external_id", request.getExternalId().get(), false);
        }
        if (request.getLimit().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "limit", request.getLimit().get(), false);
        }
        if (request.getAfter().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "after", request.getAfter().get(), false);
        }
        if (request.getBefore().isPresent()) {
            QueryStringMapper.addQueryParameter(
                    httpUrl, "before", request.getBefore().get(), false);
        }
        if (requestOptions != null) {
            requestOptions.getQueryParameters().forEach((_key, _value) -> {
                httpUrl.addQueryParameter(_key, _value);
            });
        }
        Request.Builder _requestBuilder = new Request.Builder()
                .url(httpUrl.build())
                .method("GET", null)
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Accept", "application/json");
        Request okhttpRequest = _requestBuilder.build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        if (requestOptions != null && requestOptions.getMaxRetries().isPresent()) {
            okhttpRequest = okhttpRequest
                    .newBuilder()
                    .tag(
                            RetryInterceptor.MaxRetriesOverride.class,
                            new RetryInterceptor.MaxRetriesOverride(
                                    requestOptions.getMaxRetries().get()))
                    .build();
        }
        try (Response response = client.newCall(okhttpRequest).execute()) {
            ResponseBody responseBody = response.body();
            String responseBodyString = responseBody != null ? responseBody.string() : "{}";
            if (response.isSuccessful()) {
                return new DocusealClientHttpResponse<>(
                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, SubmitterList.class), response);
            }
            Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
            throw new DocusealClientApiException(
                    "Error with status code " + response.code(), response.code(), errorBody, response);
        } catch (JsonProcessingException e) {
            throw new DocusealClientException("Failed to deserialize response: " + e.getMessage(), e);
        } catch (IOException e) {
            throw new DocusealClientException("Network error executing HTTP request", e);
        }
    }

    /**
     * The API endpoint allows you to add, remove or replace documents in the template with provided PDF/DOCX file or HTML content.
     */
    public DocusealClientHttpResponse<Template> updateTemplateDocuments(int id) {
        return updateTemplateDocuments(
                id, UpdateTemplateDocumentsParams.builder().build());
    }

    /**
     * The API endpoint allows you to add, remove or replace documents in the template with provided PDF/DOCX file or HTML content.
     */
    public DocusealClientHttpResponse<Template> updateTemplateDocuments(int id, RequestOptions requestOptions) {
        return updateTemplateDocuments(
                id, UpdateTemplateDocumentsParams.builder().build(), requestOptions);
    }

    /**
     * The API endpoint allows you to add, remove or replace documents in the template with provided PDF/DOCX file or HTML content.
     */
    public DocusealClientHttpResponse<Template> updateTemplateDocuments(int id, UpdateTemplateDocumentsParams request) {
        return updateTemplateDocuments(id, request, null);
    }

    /**
     * The API endpoint allows you to add, remove or replace documents in the template with provided PDF/DOCX file or HTML content.
     */
    public DocusealClientHttpResponse<Template> updateTemplateDocuments(
            int id, UpdateTemplateDocumentsParams request, RequestOptions requestOptions) {
        HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl())
                .newBuilder()
                .addPathSegments("templates")
                .addPathSegment(Integer.toString(id))
                .addPathSegments("documents");
        if (requestOptions != null) {
            requestOptions.getQueryParameters().forEach((_key, _value) -> {
                httpUrl.addQueryParameter(_key, _value);
            });
        }
        RequestBody body;
        try {
            body = RequestBody.create(
                    ObjectMappers.JSON_MAPPER.writeValueAsBytes(request), MediaTypes.APPLICATION_JSON);
        } catch (JsonProcessingException e) {
            throw new DocusealClientException("Failed to serialize request", e);
        }
        Request okhttpRequest = new Request.Builder()
                .url(httpUrl.build())
                .method("PUT", body)
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Content-Type", "application/json")
                .addHeader("Accept", "application/json")
                .build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        if (requestOptions != null && requestOptions.getMaxRetries().isPresent()) {
            okhttpRequest = okhttpRequest
                    .newBuilder()
                    .tag(
                            RetryInterceptor.MaxRetriesOverride.class,
                            new RetryInterceptor.MaxRetriesOverride(
                                    requestOptions.getMaxRetries().get()))
                    .build();
        }
        try (Response response = client.newCall(okhttpRequest).execute()) {
            ResponseBody responseBody = response.body();
            String responseBodyString = responseBody != null ? responseBody.string() : "{}";
            if (response.isSuccessful()) {
                return new DocusealClientHttpResponse<>(
                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Template.class), response);
            }
            Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
            throw new DocusealClientApiException(
                    "Error with status code " + response.code(), response.code(), errorBody, response);
        } catch (JsonProcessingException e) {
            throw new DocusealClientException("Failed to deserialize response: " + e.getMessage(), e);
        } catch (IOException e) {
            throw new DocusealClientException("Network error executing HTTP request", e);
        }
    }

    /**
     * The API endpoint allows you to clone an existing template into a new template.
     */
    public DocusealClientHttpResponse<Template> cloneTemplate(int id) {
        return cloneTemplate(id, CloneTemplateParams.builder().build());
    }

    /**
     * The API endpoint allows you to clone an existing template into a new template.
     */
    public DocusealClientHttpResponse<Template> cloneTemplate(int id, RequestOptions requestOptions) {
        return cloneTemplate(id, CloneTemplateParams.builder().build(), requestOptions);
    }

    /**
     * The API endpoint allows you to clone an existing template into a new template.
     */
    public DocusealClientHttpResponse<Template> cloneTemplate(int id, CloneTemplateParams request) {
        return cloneTemplate(id, request, null);
    }

    /**
     * The API endpoint allows you to clone an existing template into a new template.
     */
    public DocusealClientHttpResponse<Template> cloneTemplate(
            int id, CloneTemplateParams request, RequestOptions requestOptions) {
        HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl())
                .newBuilder()
                .addPathSegments("templates")
                .addPathSegment(Integer.toString(id))
                .addPathSegments("clone");
        if (requestOptions != null) {
            requestOptions.getQueryParameters().forEach((_key, _value) -> {
                httpUrl.addQueryParameter(_key, _value);
            });
        }
        RequestBody body;
        try {
            body = RequestBody.create(
                    ObjectMappers.JSON_MAPPER.writeValueAsBytes(request), MediaTypes.APPLICATION_JSON);
        } catch (JsonProcessingException e) {
            throw new DocusealClientException("Failed to serialize request", e);
        }
        Request okhttpRequest = new Request.Builder()
                .url(httpUrl.build())
                .method("POST", body)
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Content-Type", "application/json")
                .addHeader("Accept", "application/json")
                .build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        if (requestOptions != null && requestOptions.getMaxRetries().isPresent()) {
            okhttpRequest = okhttpRequest
                    .newBuilder()
                    .tag(
                            RetryInterceptor.MaxRetriesOverride.class,
                            new RetryInterceptor.MaxRetriesOverride(
                                    requestOptions.getMaxRetries().get()))
                    .build();
        }
        try (Response response = client.newCall(okhttpRequest).execute()) {
            ResponseBody responseBody = response.body();
            String responseBodyString = responseBody != null ? responseBody.string() : "{}";
            if (response.isSuccessful()) {
                return new DocusealClientHttpResponse<>(
                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Template.class), response);
            }
            Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
            throw new DocusealClientApiException(
                    "Error with status code " + response.code(), response.code(), errorBody, response);
        } catch (JsonProcessingException e) {
            throw new DocusealClientException("Failed to deserialize response: " + e.getMessage(), e);
        } catch (IOException e) {
            throw new DocusealClientException("Network error executing HTTP request", e);
        }
    }

    /**
     * The API endpoint provides the functionality to seamlessly generate a PDF document template by utilizing the provided HTML content while incorporating pre-defined fields.<br><b>Related Guides</b><br><a href="https://www.docuseal.com/guides/create-pdf-document-fillable-form-with-html-api" class="link">Create PDF document fillable form with HTML</a>
     */
    public DocusealClientHttpResponse<Template> createTemplateFromHtml() {
        return createTemplateFromHtml(CreateTemplateFromHtmlParams.builder().build());
    }

    /**
     * The API endpoint provides the functionality to seamlessly generate a PDF document template by utilizing the provided HTML content while incorporating pre-defined fields.<br><b>Related Guides</b><br><a href="https://www.docuseal.com/guides/create-pdf-document-fillable-form-with-html-api" class="link">Create PDF document fillable form with HTML</a>
     */
    public DocusealClientHttpResponse<Template> createTemplateFromHtml(RequestOptions requestOptions) {
        return createTemplateFromHtml(CreateTemplateFromHtmlParams.builder().build(), requestOptions);
    }

    /**
     * The API endpoint provides the functionality to seamlessly generate a PDF document template by utilizing the provided HTML content while incorporating pre-defined fields.<br><b>Related Guides</b><br><a href="https://www.docuseal.com/guides/create-pdf-document-fillable-form-with-html-api" class="link">Create PDF document fillable form with HTML</a>
     */
    public DocusealClientHttpResponse<Template> createTemplateFromHtml(CreateTemplateFromHtmlParams request) {
        return createTemplateFromHtml(request, null);
    }

    /**
     * The API endpoint provides the functionality to seamlessly generate a PDF document template by utilizing the provided HTML content while incorporating pre-defined fields.<br><b>Related Guides</b><br><a href="https://www.docuseal.com/guides/create-pdf-document-fillable-form-with-html-api" class="link">Create PDF document fillable form with HTML</a>
     */
    public DocusealClientHttpResponse<Template> createTemplateFromHtml(
            CreateTemplateFromHtmlParams request, RequestOptions requestOptions) {
        HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl())
                .newBuilder()
                .addPathSegments("templates/html");
        if (requestOptions != null) {
            requestOptions.getQueryParameters().forEach((_key, _value) -> {
                httpUrl.addQueryParameter(_key, _value);
            });
        }
        RequestBody body;
        try {
            body = RequestBody.create(
                    ObjectMappers.JSON_MAPPER.writeValueAsBytes(request), MediaTypes.APPLICATION_JSON);
        } catch (JsonProcessingException e) {
            throw new DocusealClientException("Failed to serialize request", e);
        }
        Request okhttpRequest = new Request.Builder()
                .url(httpUrl.build())
                .method("POST", body)
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Content-Type", "application/json")
                .addHeader("Accept", "application/json")
                .build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        if (requestOptions != null && requestOptions.getMaxRetries().isPresent()) {
            okhttpRequest = okhttpRequest
                    .newBuilder()
                    .tag(
                            RetryInterceptor.MaxRetriesOverride.class,
                            new RetryInterceptor.MaxRetriesOverride(
                                    requestOptions.getMaxRetries().get()))
                    .build();
        }
        try (Response response = client.newCall(okhttpRequest).execute()) {
            ResponseBody responseBody = response.body();
            String responseBodyString = responseBody != null ? responseBody.string() : "{}";
            if (response.isSuccessful()) {
                return new DocusealClientHttpResponse<>(
                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Template.class), response);
            }
            Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
            throw new DocusealClientApiException(
                    "Error with status code " + response.code(), response.code(), errorBody, response);
        } catch (JsonProcessingException e) {
            throw new DocusealClientException("Failed to deserialize response: " + e.getMessage(), e);
        } catch (IOException e) {
            throw new DocusealClientException("Network error executing HTTP request", e);
        }
    }

    /**
     * The API endpoint provides the functionality to create a fillable document template for an existing Microsoft Word document. Use <code>{{Field Name;role=Signer1;type=date}}</code> text tags to define fillable fields in the document. See <a href="https://www.docuseal.com/examples/fieldtags.docx" target="_blank" class="link font-bold" >https://www.docuseal.com/examples/fieldtags.docx</a> for more text tag formats. Or specify the exact pixel coordinates of the document fields using <code>fields</code> param.<br><b>Related Guides</b><br><a href="https://www.docuseal.com/guides/use-embedded-text-field-tags-in-the-pdf-to-create-a-fillable-form" class="link">Use embedded text field tags to create a fillable form</a>
     */
    public DocusealClientHttpResponse<Template> createTemplateFromDocx(CreateTemplateFromDocxParams request) {
        return createTemplateFromDocx(request, null);
    }

    /**
     * The API endpoint provides the functionality to create a fillable document template for an existing Microsoft Word document. Use <code>{{Field Name;role=Signer1;type=date}}</code> text tags to define fillable fields in the document. See <a href="https://www.docuseal.com/examples/fieldtags.docx" target="_blank" class="link font-bold" >https://www.docuseal.com/examples/fieldtags.docx</a> for more text tag formats. Or specify the exact pixel coordinates of the document fields using <code>fields</code> param.<br><b>Related Guides</b><br><a href="https://www.docuseal.com/guides/use-embedded-text-field-tags-in-the-pdf-to-create-a-fillable-form" class="link">Use embedded text field tags to create a fillable form</a>
     */
    public DocusealClientHttpResponse<Template> createTemplateFromDocx(
            CreateTemplateFromDocxParams request, RequestOptions requestOptions) {
        HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl())
                .newBuilder()
                .addPathSegments("templates/docx");
        if (requestOptions != null) {
            requestOptions.getQueryParameters().forEach((_key, _value) -> {
                httpUrl.addQueryParameter(_key, _value);
            });
        }
        RequestBody body;
        try {
            body = RequestBody.create(
                    ObjectMappers.JSON_MAPPER.writeValueAsBytes(request), MediaTypes.APPLICATION_JSON);
        } catch (JsonProcessingException e) {
            throw new DocusealClientException("Failed to serialize request", e);
        }
        Request okhttpRequest = new Request.Builder()
                .url(httpUrl.build())
                .method("POST", body)
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Content-Type", "application/json")
                .addHeader("Accept", "application/json")
                .build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        if (requestOptions != null && requestOptions.getMaxRetries().isPresent()) {
            okhttpRequest = okhttpRequest
                    .newBuilder()
                    .tag(
                            RetryInterceptor.MaxRetriesOverride.class,
                            new RetryInterceptor.MaxRetriesOverride(
                                    requestOptions.getMaxRetries().get()))
                    .build();
        }
        try (Response response = client.newCall(okhttpRequest).execute()) {
            ResponseBody responseBody = response.body();
            String responseBodyString = responseBody != null ? responseBody.string() : "{}";
            if (response.isSuccessful()) {
                return new DocusealClientHttpResponse<>(
                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Template.class), response);
            }
            Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
            throw new DocusealClientApiException(
                    "Error with status code " + response.code(), response.code(), errorBody, response);
        } catch (JsonProcessingException e) {
            throw new DocusealClientException("Failed to deserialize response: " + e.getMessage(), e);
        } catch (IOException e) {
            throw new DocusealClientException("Network error executing HTTP request", e);
        }
    }

    /**
     * The API endpoint provides the functionality to create a fillable document template for a PDF file. Use <code>{{Field Name;role=Signer1;type=date}}</code> text tags to define fillable fields in the document. See <a href="https://www.docuseal.com/examples/fieldtags.pdf" target="_blank" class="link font-bold">https://www.docuseal.com/examples/fieldtags.pdf</a> for more text tag formats. Or specify the exact pixel coordinates of the document fields using <code>fields</code> param.<br><b>Related Guides</b><br><a href="https://www.docuseal.com/guides/use-embedded-text-field-tags-in-the-pdf-to-create-a-fillable-form" class="link">Use embedded text field tags to create a fillable form</a>
     */
    public DocusealClientHttpResponse<Template> createTemplateFromPdf(CreateTemplateFromPdfParams request) {
        return createTemplateFromPdf(request, null);
    }

    /**
     * The API endpoint provides the functionality to create a fillable document template for a PDF file. Use <code>{{Field Name;role=Signer1;type=date}}</code> text tags to define fillable fields in the document. See <a href="https://www.docuseal.com/examples/fieldtags.pdf" target="_blank" class="link font-bold">https://www.docuseal.com/examples/fieldtags.pdf</a> for more text tag formats. Or specify the exact pixel coordinates of the document fields using <code>fields</code> param.<br><b>Related Guides</b><br><a href="https://www.docuseal.com/guides/use-embedded-text-field-tags-in-the-pdf-to-create-a-fillable-form" class="link">Use embedded text field tags to create a fillable form</a>
     */
    public DocusealClientHttpResponse<Template> createTemplateFromPdf(
            CreateTemplateFromPdfParams request, RequestOptions requestOptions) {
        HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl())
                .newBuilder()
                .addPathSegments("templates/pdf");
        if (requestOptions != null) {
            requestOptions.getQueryParameters().forEach((_key, _value) -> {
                httpUrl.addQueryParameter(_key, _value);
            });
        }
        RequestBody body;
        try {
            body = RequestBody.create(
                    ObjectMappers.JSON_MAPPER.writeValueAsBytes(request), MediaTypes.APPLICATION_JSON);
        } catch (JsonProcessingException e) {
            throw new DocusealClientException("Failed to serialize request", e);
        }
        Request okhttpRequest = new Request.Builder()
                .url(httpUrl.build())
                .method("POST", body)
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Content-Type", "application/json")
                .addHeader("Accept", "application/json")
                .build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        if (requestOptions != null && requestOptions.getMaxRetries().isPresent()) {
            okhttpRequest = okhttpRequest
                    .newBuilder()
                    .tag(
                            RetryInterceptor.MaxRetriesOverride.class,
                            new RetryInterceptor.MaxRetriesOverride(
                                    requestOptions.getMaxRetries().get()))
                    .build();
        }
        try (Response response = client.newCall(okhttpRequest).execute()) {
            ResponseBody responseBody = response.body();
            String responseBodyString = responseBody != null ? responseBody.string() : "{}";
            if (response.isSuccessful()) {
                return new DocusealClientHttpResponse<>(
                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Template.class), response);
            }
            Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
            throw new DocusealClientApiException(
                    "Error with status code " + response.code(), response.code(), errorBody, response);
        } catch (JsonProcessingException e) {
            throw new DocusealClientException("Failed to deserialize response: " + e.getMessage(), e);
        } catch (IOException e) {
            throw new DocusealClientException("Network error executing HTTP request", e);
        }
    }

    /**
     * The API endpoint allows you to merge multiple templates with documents and fields into a new combined template.
     */
    public DocusealClientHttpResponse<Template> mergeTemplate(MergeTemplateParams request) {
        return mergeTemplate(request, null);
    }

    /**
     * The API endpoint allows you to merge multiple templates with documents and fields into a new combined template.
     */
    public DocusealClientHttpResponse<Template> mergeTemplate(
            MergeTemplateParams request, RequestOptions requestOptions) {
        HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl())
                .newBuilder()
                .addPathSegments("templates/merge");
        if (requestOptions != null) {
            requestOptions.getQueryParameters().forEach((_key, _value) -> {
                httpUrl.addQueryParameter(_key, _value);
            });
        }
        RequestBody body;
        try {
            body = RequestBody.create(
                    ObjectMappers.JSON_MAPPER.writeValueAsBytes(request), MediaTypes.APPLICATION_JSON);
        } catch (JsonProcessingException e) {
            throw new DocusealClientException("Failed to serialize request", e);
        }
        Request okhttpRequest = new Request.Builder()
                .url(httpUrl.build())
                .method("POST", body)
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Content-Type", "application/json")
                .addHeader("Accept", "application/json")
                .build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        if (requestOptions != null && requestOptions.getMaxRetries().isPresent()) {
            okhttpRequest = okhttpRequest
                    .newBuilder()
                    .tag(
                            RetryInterceptor.MaxRetriesOverride.class,
                            new RetryInterceptor.MaxRetriesOverride(
                                    requestOptions.getMaxRetries().get()))
                    .build();
        }
        try (Response response = client.newCall(okhttpRequest).execute()) {
            ResponseBody responseBody = response.body();
            String responseBodyString = responseBody != null ? responseBody.string() : "{}";
            if (response.isSuccessful()) {
                return new DocusealClientHttpResponse<>(
                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, Template.class), response);
            }
            Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
            throw new DocusealClientApiException(
                    "Error with status code " + response.code(), response.code(), errorBody, response);
        } catch (JsonProcessingException e) {
            throw new DocusealClientException("Failed to deserialize response: " + e.getMessage(), e);
        } catch (IOException e) {
            throw new DocusealClientException("Network error executing HTTP request", e);
        }
    }

    /**
     * This API endpoint allows you to create signature requests (submissions) for a document template and send them to the specified submitters (signers).<br><b>Related Guides</b><br><a href="https://www.docuseal.com/guides/send-documents-for-signature-via-api" class="link">Send documents for signature via API</a><br><a href="https://www.docuseal.com/guides/pre-fill-pdf-document-form-fields-with-api" class="link">Pre-fill PDF document form fields with API</a>
     */
    public DocusealClientHttpResponse<SubmissionCreateResult> createSubmission(CreateSubmissionParams request) {
        return createSubmission(request, null);
    }

    /**
     * This API endpoint allows you to create signature requests (submissions) for a document template and send them to the specified submitters (signers).<br><b>Related Guides</b><br><a href="https://www.docuseal.com/guides/send-documents-for-signature-via-api" class="link">Send documents for signature via API</a><br><a href="https://www.docuseal.com/guides/pre-fill-pdf-document-form-fields-with-api" class="link">Pre-fill PDF document form fields with API</a>
     */
    public DocusealClientHttpResponse<SubmissionCreateResult> createSubmission(
            CreateSubmissionParams request, RequestOptions requestOptions) {
        HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl())
                .newBuilder()
                .addPathSegments("submissions/init");
        if (requestOptions != null) {
            requestOptions.getQueryParameters().forEach((_key, _value) -> {
                httpUrl.addQueryParameter(_key, _value);
            });
        }
        RequestBody body;
        try {
            body = RequestBody.create(
                    ObjectMappers.JSON_MAPPER.writeValueAsBytes(request), MediaTypes.APPLICATION_JSON);
        } catch (JsonProcessingException e) {
            throw new DocusealClientException("Failed to serialize request", e);
        }
        Request okhttpRequest = new Request.Builder()
                .url(httpUrl.build())
                .method("POST", body)
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Content-Type", "application/json")
                .addHeader("Accept", "application/json")
                .build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        if (requestOptions != null && requestOptions.getMaxRetries().isPresent()) {
            okhttpRequest = okhttpRequest
                    .newBuilder()
                    .tag(
                            RetryInterceptor.MaxRetriesOverride.class,
                            new RetryInterceptor.MaxRetriesOverride(
                                    requestOptions.getMaxRetries().get()))
                    .build();
        }
        try (Response response = client.newCall(okhttpRequest).execute()) {
            ResponseBody responseBody = response.body();
            String responseBodyString = responseBody != null ? responseBody.string() : "{}";
            if (response.isSuccessful()) {
                return new DocusealClientHttpResponse<>(
                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, SubmissionCreateResult.class),
                        response);
            }
            Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
            throw new DocusealClientApiException(
                    "Error with status code " + response.code(), response.code(), errorBody, response);
        } catch (JsonProcessingException e) {
            throw new DocusealClientException("Failed to deserialize response: " + e.getMessage(), e);
        } catch (IOException e) {
            throw new DocusealClientException("Network error executing HTTP request", e);
        }
    }

    /**
     * The API endpoint allows you to permanently delete a document template and all of its submissions.
     */
    public DocusealClientHttpResponse<TemplatePermanentlyDeleteResult> permanentlyDeleteTemplate(int id) {
        return permanentlyDeleteTemplate(
                id, PermanentlyDeleteTemplateParams.builder().build());
    }

    /**
     * The API endpoint allows you to permanently delete a document template and all of its submissions.
     */
    public DocusealClientHttpResponse<TemplatePermanentlyDeleteResult> permanentlyDeleteTemplate(
            int id, RequestOptions requestOptions) {
        return permanentlyDeleteTemplate(
                id, PermanentlyDeleteTemplateParams.builder().build(), requestOptions);
    }

    /**
     * The API endpoint allows you to permanently delete a document template and all of its submissions.
     */
    public DocusealClientHttpResponse<TemplatePermanentlyDeleteResult> permanentlyDeleteTemplate(
            int id, PermanentlyDeleteTemplateParams request) {
        return permanentlyDeleteTemplate(id, request, null);
    }

    /**
     * The API endpoint allows you to permanently delete a document template and all of its submissions.
     */
    public DocusealClientHttpResponse<TemplatePermanentlyDeleteResult> permanentlyDeleteTemplate(
            int id, PermanentlyDeleteTemplateParams request, RequestOptions requestOptions) {
        HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl())
                .newBuilder()
                .addPathSegments("templates")
                .addPathSegment(Integer.toString(id))
                .addQueryParameter("permanently", "true");
        if (requestOptions != null) {
            requestOptions.getQueryParameters().forEach((_key, _value) -> {
                httpUrl.addQueryParameter(_key, _value);
            });
        }
        Request.Builder _requestBuilder = new Request.Builder()
                .url(httpUrl.build())
                .method("DELETE", null)
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Accept", "application/json");
        Request okhttpRequest = _requestBuilder.build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        if (requestOptions != null && requestOptions.getMaxRetries().isPresent()) {
            okhttpRequest = okhttpRequest
                    .newBuilder()
                    .tag(
                            RetryInterceptor.MaxRetriesOverride.class,
                            new RetryInterceptor.MaxRetriesOverride(
                                    requestOptions.getMaxRetries().get()))
                    .build();
        }
        try (Response response = client.newCall(okhttpRequest).execute()) {
            ResponseBody responseBody = response.body();
            String responseBodyString = responseBody != null ? responseBody.string() : "{}";
            if (response.isSuccessful()) {
                return new DocusealClientHttpResponse<>(
                        ObjectMappers.JSON_MAPPER.readValue(responseBodyString, TemplatePermanentlyDeleteResult.class),
                        response);
            }
            Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
            throw new DocusealClientApiException(
                    "Error with status code " + response.code(), response.code(), errorBody, response);
        } catch (JsonProcessingException e) {
            throw new DocusealClientException("Failed to deserialize response: " + e.getMessage(), e);
        } catch (IOException e) {
            throw new DocusealClientException("Network error executing HTTP request", e);
        }
    }

    /**
     * The API endpoint allows you to permanently delete a submission and all of its submitters and documents.
     */
    public DocusealClientHttpResponse<SubmissionPermanentlyDeleteResult> permanentlyDeleteSubmission(int id) {
        return permanentlyDeleteSubmission(
                id, PermanentlyDeleteSubmissionParams.builder().build());
    }

    /**
     * The API endpoint allows you to permanently delete a submission and all of its submitters and documents.
     */
    public DocusealClientHttpResponse<SubmissionPermanentlyDeleteResult> permanentlyDeleteSubmission(
            int id, RequestOptions requestOptions) {
        return permanentlyDeleteSubmission(
                id, PermanentlyDeleteSubmissionParams.builder().build(), requestOptions);
    }

    /**
     * The API endpoint allows you to permanently delete a submission and all of its submitters and documents.
     */
    public DocusealClientHttpResponse<SubmissionPermanentlyDeleteResult> permanentlyDeleteSubmission(
            int id, PermanentlyDeleteSubmissionParams request) {
        return permanentlyDeleteSubmission(id, request, null);
    }

    /**
     * The API endpoint allows you to permanently delete a submission and all of its submitters and documents.
     */
    public DocusealClientHttpResponse<SubmissionPermanentlyDeleteResult> permanentlyDeleteSubmission(
            int id, PermanentlyDeleteSubmissionParams request, RequestOptions requestOptions) {
        HttpUrl.Builder httpUrl = HttpUrl.parse(this.clientOptions.environment().getUrl())
                .newBuilder()
                .addPathSegments("submissions")
                .addPathSegment(Integer.toString(id))
                .addQueryParameter("permanently", "true");
        if (requestOptions != null) {
            requestOptions.getQueryParameters().forEach((_key, _value) -> {
                httpUrl.addQueryParameter(_key, _value);
            });
        }
        Request.Builder _requestBuilder = new Request.Builder()
                .url(httpUrl.build())
                .method("DELETE", null)
                .headers(Headers.of(clientOptions.headers(requestOptions)))
                .addHeader("Accept", "application/json");
        Request okhttpRequest = _requestBuilder.build();
        OkHttpClient client = clientOptions.httpClient();
        if (requestOptions != null && requestOptions.getTimeout().isPresent()) {
            client = clientOptions.httpClientWithTimeout(requestOptions);
        }
        if (requestOptions != null && requestOptions.getMaxRetries().isPresent()) {
            okhttpRequest = okhttpRequest
                    .newBuilder()
                    .tag(
                            RetryInterceptor.MaxRetriesOverride.class,
                            new RetryInterceptor.MaxRetriesOverride(
                                    requestOptions.getMaxRetries().get()))
                    .build();
        }
        try (Response response = client.newCall(okhttpRequest).execute()) {
            ResponseBody responseBody = response.body();
            String responseBodyString = responseBody != null ? responseBody.string() : "{}";
            if (response.isSuccessful()) {
                return new DocusealClientHttpResponse<>(
                        ObjectMappers.JSON_MAPPER.readValue(
                                responseBodyString, SubmissionPermanentlyDeleteResult.class),
                        response);
            }
            Object errorBody = ObjectMappers.parseErrorBody(responseBodyString);
            throw new DocusealClientApiException(
                    "Error with status code " + response.code(), response.code(), errorBody, response);
        } catch (JsonProcessingException e) {
            throw new DocusealClientException("Failed to deserialize response: " + e.getMessage(), e);
        } catch (IOException e) {
            throw new DocusealClientException("Network error executing HTTP request", e);
        }
    }
}
