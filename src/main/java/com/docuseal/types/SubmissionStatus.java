/**
 * This file was auto-generated by Fern from our API Definition.
 */
package com.docuseal.types;

import com.fasterxml.jackson.annotation.JsonCreator;
import com.fasterxml.jackson.annotation.JsonValue;

public final class SubmissionStatus {
    public static final SubmissionStatus DECLINED = new SubmissionStatus(Value.DECLINED, "declined");

    public static final SubmissionStatus PENDING = new SubmissionStatus(Value.PENDING, "pending");

    public static final SubmissionStatus COMPLETED = new SubmissionStatus(Value.COMPLETED, "completed");

    public static final SubmissionStatus EXPIRED = new SubmissionStatus(Value.EXPIRED, "expired");

    private final Value value;

    private final String string;

    SubmissionStatus(Value value, String string) {
        this.value = value;
        this.string = string;
    }

    public Value getEnumValue() {
        return value;
    }

    @java.lang.Override
    @JsonValue
    public String toString() {
        return this.string;
    }

    @java.lang.Override
    public boolean equals(Object other) {
        return (this == other)
                || (other instanceof SubmissionStatus && this.string.equals(((SubmissionStatus) other).string));
    }

    @java.lang.Override
    public int hashCode() {
        return this.string.hashCode();
    }

    public <T> T visit(Visitor<T> visitor) {
        switch (value) {
            case DECLINED:
                return visitor.visitDeclined();
            case PENDING:
                return visitor.visitPending();
            case COMPLETED:
                return visitor.visitCompleted();
            case EXPIRED:
                return visitor.visitExpired();
            case UNKNOWN:
            default:
                return visitor.visitUnknown(string);
        }
    }

    @JsonCreator(mode = JsonCreator.Mode.DELEGATING)
    public static SubmissionStatus valueOf(String value) {
        switch (value) {
            case "declined":
                return DECLINED;
            case "pending":
                return PENDING;
            case "completed":
                return COMPLETED;
            case "expired":
                return EXPIRED;
            default:
                return new SubmissionStatus(Value.UNKNOWN, value);
        }
    }

    public enum Value {
        COMPLETED,

        DECLINED,

        EXPIRED,

        PENDING,

        UNKNOWN
    }

    public interface Visitor<T> {
        T visitCompleted();

        T visitDeclined();

        T visitExpired();

        T visitPending();

        T visitUnknown(String unknownType);
    }
}
